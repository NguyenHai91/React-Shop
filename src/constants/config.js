
export const serverURL = `hainguyen91.pythonanywhere.com`;

export const endpoint = `${serverURL}`;
export const usersURL = `${endpoint}/users`;
export const productsURL = `${endpoint}/products`;
export const cartURL = `${endpoint}/carts`;